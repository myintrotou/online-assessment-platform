export const environment = {
  production: true,
  // Replaced at deploy time by the Render build (from the API_URL env var).
  // For a manual build, set this to your deployed API origin.
  apiBaseUrl: '__API_URL__',
};
